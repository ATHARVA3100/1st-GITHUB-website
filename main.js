
 
  
  var result;
  
   function Tables() {
    a = prompt("Enter n for it's tables")
      a = Number.parseInt(a)
      b = prompt("Enter b for extent of tables")
      b = Number.parseInt(b)
      for (let i = 1; i <= b; i++) {
        document.write(a + "X" + i + "=" + a * i + "<Br>")
      }}

      function Multiplication(){
     a = prompt("Enter n for it's Multiplication")
      a = Number.parseInt(a)
      b = prompt("Enter b for it's Multiplication")
      b = Number.parseInt(b)
      document.write(a+" X "+ b +"=" +a * b)}
      
    function Remainder(){a = prompt("Enter divident")
      a = Number.parseInt(a)
      b = prompt("Enter divisor")
      b = Number.parseInt(b)
      document.write(a +" % "+ b +"="+a % b)}
      
    function Division(){a = prompt("Enter divident")
      a = Number.parseInt(a)
      b = prompt("Enter divisor")
      b = Number.parseInt(b)
       document.write(a +" / "+b +"="+a / b)}

  
    
  






